"""moveType.py — Enum for the three possible moves in Rock Paper Scissors."""
from enum import Enum

class MoveType(Enum):
    """Represents the three possible moves."""
    
    ROCK = 0
    PAPER = 1
    SCISSOR = 2
    